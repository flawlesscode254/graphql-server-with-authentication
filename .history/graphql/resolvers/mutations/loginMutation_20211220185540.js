const Register = require("../../../models/Register")
const {loginValidator}

module.exports = {
    Mutation: {
        login: async (_, {
            email,
            password
        }) => {

        }
    }
}